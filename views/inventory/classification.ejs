<!-- Header Section -->
<% if (title) { %>
    <h1>
        <%= title %>
    </h1>
    <% } else { %>
        <% res.redirect('/') %>
            <% } %>

                <!-- Messages Block (commented out for now) -->
               <%# messages() %>

                    <!-- Inventory Grid -->
                    <%- grid %>